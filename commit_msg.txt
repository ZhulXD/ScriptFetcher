🧪 [testing improvement] Add missing unit test for do_tree_scan with empty services

🎯 **What:** The testing gap addressed
This pull request addresses the testing gap in `Game_Context_Scanner.lua` by adding a missing unit test for `do_tree_scan` when game services (`ReplicatedStorage`, `Workspace`, `StarterGui`, and `PlayerGui`) return `nil` or are not present.

📊 **Coverage:** What scenarios are now tested
- The `do_tree_scan` loop handling when iterating over an empty or `nil` map of services.
- Mocks were introduced to return `nil` for `game:GetService` and `Players.LocalPlayer:WaitForChild`.
- The execution of `scanner.do_tree_scan({})` is wrapped in a `pcall` to ensure it gracefully completes without throwing runtime errors or exceptions when encountering missing services.

✨ **Result:** The improvement in test coverage
- Increased code reliability by guaranteeing that `do_tree_scan` can safely handle limited or locked Roblox environments (e.g., restricted plugins or strict execution contexts) where certain standard services might be inaccessible. Test suite summary shows an additional test successfully passed.
