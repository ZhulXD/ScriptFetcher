🧹 Refactor duplicated list-building logic in property handlers

🎯 What: Introduced an `add_prop` helper function to replace duplicated list-appending code (`local len = #props; len = len + 1; props[len] = ...`) across all property handlers (e.g., `handle_seat`, `handle_basepart`, etc.) in `Game_Context_Scanner.lua`.
💡 Why: This significantly improves maintainability and readability by abstracting the repetitive string concatenation and array length management.
✅ Verification: Ran `restore_and_fix.py` locally and verified that the scanner is healthy. Also manually reviewed the diff to ensure the output formatting remains exactly identical.
✨ Result: Cleaned up around 30 lines of duplicate boilerplate code, making property handlers much easier to read and extend.
